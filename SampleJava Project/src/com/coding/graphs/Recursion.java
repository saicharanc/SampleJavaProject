package com.coding.graphs;

import java.io.File;

/**
 * A function that calls itself is a recursive function.
 * @author SaiCharanChalasani
 * Example of a recursive function to find the number of txt files in a computer.
 */

/*public class Recursion {

	public int countTxt(Directory dir){
		
		if(dir == null){
			return 0;
		}
		
		int count;
		
		for(File file : dir){
			if(file.endsWith(".txt")){
				count ++;
			}
		}
		
		for (Directory subDir : dir){
			count + = countTxt(subDir);
		}
	} 
	 return count;
	
}
*/
